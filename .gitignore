# Dependencies
node_modules/

# Build output
dist/
release/

# IDE
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local

# update-config.json: runtime config bundled by electron-builder
# (extraResources in package.json). Currently empty `{}` — tracked so
# CI builds find it. If it later carries a token, move that token into
# a separate gitignored file and keep this one as `{}`.

# Editor directories
*.swp
*.swo
*~
